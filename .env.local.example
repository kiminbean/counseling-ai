# Counseling AI Frontend - Environment Variables Example
# Copy this file to .env.local and fill in your values

# API Configuration
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Environment
NEXT_PUBLIC_ENVIRONMENT=development

# -----------------------------------------------------------------------------
# Sentry Configuration (Production Error Tracking)
# -----------------------------------------------------------------------------
# Get these values from https://sentry.io/
# Leave empty to disable Sentry (console logging will be used as fallback)

# Required for error tracking
NEXT_PUBLIC_SENTRY_DSN=

# Required for source map uploads (optional - for better stack traces)
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
