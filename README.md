# Counseling AI

Integrated AI Counseling Platform supporting Text (Phase 1), Voice (Phase 2), and Multilingual/Supervisor features (Phase 3).

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements_phase3.txt
   ```

2. Run API:
   ```bash
   uvicorn api.main:app --reload
   ```
